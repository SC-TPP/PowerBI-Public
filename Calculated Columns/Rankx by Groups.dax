Rank Descending = 
                Rankx(
                    Filter(
                        Table,
                        Table[Group1] = Earlier(Table[Group1]) && Table[Group2] = Earlier(Table[Group2])
                    ),
                        Table[Value],
                        ,
                        DESC,
                        Skip
                    )